# Root env — used by Docker Compose (optional for local dev)
# Copy to .env in the project root when using `docker compose up`

JWT_SECRET=dev-secret-change-me

# Same Client ID as frontend REACT_APP_GOOGLE_CLIENT_ID and backend GOOGLE_CLIENT_ID
GOOGLE_CLIENT_ID=
